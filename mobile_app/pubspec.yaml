name: geo_task_flutter_app
description: "A new Flutter project for geo task assignments."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  http: ^1.6.0
  shared_preferences: ^2.5.5
  flutter_appauth: ^12.0.2
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
